import type { Viewport } from 'next';

export const dynamic = 'force-static';

export const viewport: Viewport = {
  themeColor: '#4F7942',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};
